# Example `.env.example` — safe to commit. Copy to `.env` and fill values locally.
NODE_ENV=development
PORT=5000
MONGO_URI=your_mongo_uri_here
JWT_SECRET=your_jwt_secret_here
NEXT_PUBLIC_API_URL=http://localhost:5000

# Do NOT commit real secrets. Store secrets in a secure secret manager or GitHub Actions secrets.